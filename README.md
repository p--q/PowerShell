本番環境は管理者権限不可。

まず許可コマンドでps1の実行を許可する。

Check_PS_Environment.ps1で実行環境の確認。

git stash 文字エンコードしているためそのままではpullできないので。

git pull

Fix-BOM.ps1で文字エンコードを変更。

TaskbarShortcutMaker.ps1を起動してps1ファイルのショートカットをデスクトップに作成。

ps1ファイルのショートカットをタスクバーにピン留め。

整形したい文字列をクリップボードに入れて

左端からWin+順番数字で起動可能。
